<div id="review" class="accounts-review">
  <h2>Accounts review for <%= @date %></h2>

  <% if @items.empty? %>
    <p>No items bought in <%= @date %> to show.</p>
  <% else %>
    <%= render 'review' %>
  <% end %>
</div>
